# 安全策略

## 支持范围

本仓库内的**技能文档本身**（Markdown 文件、PowerShell 模板、Python 脚本）是支持范围。**这些脚本在用户机器上执行的具体操作不在支持范围内**——执行结果取决于用户输入和环境。

## 报告漏洞

发现安全漏洞（如：硬编码密钥、脚本注入、可执行命令注入路径等）请通过 GitHub Private Vulnerability Reporting 提交：

1. 仓库页面 → Security tab → "Report a vulnerability"
2. 或邮件联系仓库所有者

### 报告内容

- 漏洞位置（文件 + 行号）
- 漏洞描述（如何触发、影响范围）
- 复现步骤（最小命令）
- 修复建议（可选）

### 响应时间

- 工作日 72 小时内确认收到
- 严重性评估在 7 天内完成
- 修复时间视严重性而定

## 安全原则

本技能本身遵循的"零伤害"安全原则：

- **不修改 C:\Windows、C:\Program Files**（除非用户明确授权）
- **所有清理走回收站**（不永久删除）
- **所有操作可回退**
- **非管理员权限能做的，绝不用管理员权限**

如果你发现脚本违反这些原则，请报告——这是 skill 自身的 bug。
